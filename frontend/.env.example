# Optional: Set an explicit backend API URL override.
# If left commented or blank, the frontend dynamically matches the current browser hostname (e.g. http://192.168.100.63:8000).
# VITE_API_BASE_URL=http://127.0.0.1:8000
